# Model Usage

OpenCode plugin for tracking and visualizing model usage, token consumption, and costs.

## Structure

- `model-usage.tsx` — TUI plugin entry point
- `model-usage-server.ts` — Server plugin entry point
- `model-usage/` — Sub-modules (helpers, shared, types, etc.)
- `tests/` — Unit tests
